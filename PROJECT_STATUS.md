# 秘钥守望者项目状态与接手报告

更新时间：2026-08-20

本文档记录当前真实完成度、运行状态、设计边界和后续工作，供维护者直接接手。
报告不包含服务器地址、访问凭据、受影响目标、完整密钥或可利用证据。

## 1. 项目目标

秘钥守望者是公益 AI API 凭据暴露发现与负责任披露项目。系统从合规测绘数据取得
资产线索，主动读取公开可访问的 HTML 与同源 JavaScript 构建产物，发现其中可能
暴露的大模型凭据和配置，再以最小化方式保存证据。

项目不调用发现的密钥、不查询余额、不绕过认证、不公开目标清单。

## 2. 当前结论

核心 MVP 已实现并部署，但真实发现闭环尚未打通。

- 已观察资产：120
- 应用指纹命中：120
- 已识别产品：6
- 第一批主动复核：30 个资产
- 第一批有效 JS 响应字节：0
- 真实凭据发现：0
- 自动化测试：10 项通过

第一批扫描任务本身成功结束，但目标入口没有返回可分析的 200 响应，因此不能宣称
已经发现密钥。下一阶段首先要解决资产 URL 构造、HTTP 状态诊断、重定向、TLS、
Host/SNI、WAF 和真实前端入口定位。

## 3. 已完成能力

### 3.1 资产发现与应用识别

- 通过 FOFA API 导入资产元数据。
- SQLite 对资产去重并记录首次、最近观察时间。
- 已有 14 种应用或网关指纹，包括 DeepTutor、Dify、Open WebUI、AnythingLLM、
  LobeChat、LibreChat、FastGPT、RAGFlow、New API、One API、LiteLLM、NextChat、
  Ollama 和 vLLM。
- FOFA 导入当前由主节点每小时运行一次。

### 3.2 JavaScript 暴露发现框架

- 从入口 HTML 解析 `script` 和 `link` 资源。
- 发现同源 `.js`、`.mjs`、动态 chunk 和 `.map` 引用。
- 支持 `sourceMappingURL`。
- 限制同源、文件数量、单文件大小和总下载量。
- 拒绝私网、环回、链路本地、组播和保留地址，降低 SSRF 风险。
- 使用可识别的公益项目 User-Agent，不绕过访问控制。

### 3.3 凭据识别与数据最小化

首批上下文规则覆盖：

- DeepSeek
- 阿里云百炼／通义千问
- 火山引擎方舟／豆包
- 智谱 GLM
- 月之暗面 Kimi
- 百度千帆／文心
- 腾讯混元
- MiniMax
- 百川智能
- 零一万物
- 硅基流动

提供商归属不是只看 `sk-` 前缀，而是结合环境变量、API Base URL、模型名和附近
代码上下文。没有提供商上下文的通用 `sk-` 不会强制归类。

完整候选密钥仅在函数局部内存中短暂存在。持久化数据仅包括：

- 提供商与产品
- 资产名称
- 来源路径
- 密钥末 8 位
- 使用服务端随机密钥生成的 HMAC-SHA256 指纹
- 置信度、风险级别、首次和最近发现时间、处置状态

完整密钥不进入数据库、普通日志、API 返回或公开报告。

### 3.4 DeepSeek AI 复核

- DeepSeek 作为可选语义复核器参与分析疑似 JavaScript。
- 请求发送前，本地替换密钥、Authorization、Cookie、Token 和敏感赋值。
- AI 只返回供应商、同源候选路径、框架和简短理由。
- AI 建议的路径仍受同源和公网地址限制。
- DeepSeek API Key 只存放在服务器受限环境文件中，不进入仓库。

### 3.5 查询与公开接口

- `GET /health`：服务健康。
- `GET /api/public/stats`：脱敏汇总统计。
- `GET /api/public/coverage`：产品指纹覆盖。
- `POST /api/public/key-lookup`：输入严格四位后缀，返回脱敏的初步匹配。
- `POST /api/admin/fofa/import`：管理令牌保护的 FOFA 导入。

末四位查询不是所有权证明。结果仅显示脱敏资产提示；后续必须增加 DNS、指定文件
或企业邮箱验证，才能展示处置信息。

## 4. 数据库

当前使用 SQLite，主要表：

- `assets`：FOFA 资产元数据。
- `detections`：产品指纹结果。
- `import_jobs`：FOFA 导入任务。
- `asset_scans`：主动扫描状态与非敏感计数。
- `credential_findings`：脱敏凭据发现。

SQLite 适合当前单调度节点 MVP，不适合三节点并行写入。迁移到 PostgreSQL 和任务队列
前，不应让三台节点同时扫描并各自写本地数据库，否则会产生重复任务和数据分叉。

## 5. 生产部署

- 三台服务器均运行 SecretWatcher Web 服务。
- 三个公网入口均提供 HTTP 到 HTTPS 跳转。
- Let’s Encrypt 证书覆盖根域与 `www`。
- 三台均有两分钟健康检查和自动恢复。
- 主节点运行每小时 FOFA 导入和每两小时 JavaScript 扫描。
- 另外两台当前主要作为 Web 副本，尚未加入扫描任务分配。
- 一台节点按运维要求同时恢复了既有 Xray 服务；它不属于本仓库。

证书由主节点自动续期。当前副本使用一次性分发的同一证书；下一次续期后仍需同步，
或改用 DNS-01 让各节点独立续期。

## 6. 配置与秘密

仓库只提供 `.env.example` 和示例 TOML。生产秘密必须放在服务器环境文件或秘密管理器：

- `FOFA_KEY`
- `SECRETWATCHER_ADMIN_TOKEN`
- `SECRETWATCHER_FINGERPRINT_KEY`
- `DEEPSEEK_API_KEY`
- `DEEPSEEK_BASE_URL`
- `DEEPSEEK_MODEL`

任何曾通过聊天、Issue、日志或公开渠道传输的密钥都应轮换。

## 7. 已知缺陷

### P0：真实 JS 内容尚未取得

首次 30 个资产均未取得可分析响应。当前扫描汇总只记录成功文件和字节，缺少按原因
聚合的诊断。需要增加不含目标地址的状态统计：DNS 失败、连接失败、TLS 失败、超时、
3xx、401、403、404、429、5xx 和内容类型不符。

### P0：FOFA URL 归一化不足

必须可靠组合协议、主机、端口和路径，并处理 IPv6、非标准端口、Host/SNI 与 FOFA
字段差异。重定向只能跟随到同源，跨域资源应进入单独策略，不可默认扩张扫描范围。

### P1：构建系统解析仍不完整

需要专门支持 Webpack runtime、Vite manifest、Next.js build manifest、懒加载 chunk、
内联 JSON 配置、转义字符串和 source map 的 `sourcesContent`。

### P1：AI 复核结果未持久化

当前 AI 输出只在单次扫描内用于增加候选路径。后续应保存脱敏的模型判断、模型版本、
提示版本和审计时间，但不得保存发送前的原始代码。

### P1：三节点尚未形成任务集群

需要 PostgreSQL、任务队列、租约、幂等键、超时回收和节点心跳。任务必须按资产域名
限速，避免不同节点同时访问同一目标。

### P1：认领与通知尚未实现

末四位查询已存在，但域名所有权验证、企业邮箱验证、通知模板、人工审批、通知记录、
修复复测、误报申诉和删除流程尚未实现。

### P2：公开主页尚未展示凭据统计

主页当前主要展示资产和产品指纹。真实发现数大于零并经过复核前，不应展示演示数据。

## 8. 推荐接手顺序

1. 为扫描器增加脱敏失败原因统计和测试。
2. 修复 FOFA 资产 URL 归一化，使用 DeepTutor 已知样本建立端到端回归。
3. 完善 HTML、Webpack、Vite、Next.js 和 source map 资源图。
4. 取得第一条真实发现后进行人工复核，确认完整密钥从未落盘或进入日志。
5. 增加管理员发现列表和安全认领流程。
6. 将 SQLite 迁移到 PostgreSQL，建立三节点任务租约。
7. 配置 DNS-01 证书独立续期。
8. 建立通知、修复、复测和删除闭环。

## 9. 本地验证

```bash
python3 -m venv .venv
.venv/bin/pip install -e '.[test]'
.venv/bin/pytest -q
```

运行 FOFA 导入和主动扫描前，先在本地私密环境中配置对应变量。不要把真实密钥写入
命令行历史、测试夹具、截图或 Git 提交。

## 10. 完成定义

项目进入可公开宣称的 MVP，需要同时满足：

- 至少一个经过人工确认的真实凭据暴露事件。
- 完整凭据不在数据库、日志、AI 请求、错误报告和导出中出现。
- 所有权验证与负责任通知能够闭环。
- 三节点任务不会重复访问同一资产。
- 有明确的限速、暂停、删除和误报处理机制。
- 公开页面只展示脱敏且不会增加受影响方风险的数据。
